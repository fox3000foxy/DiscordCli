let choices = [],
    Guild,
    Guilds,
    Channels,
    ChannelsId = new Array,
    ChannelsType,
    selectCursor = new Array(0, 0, 0),
    limit = 100,
    chat = "",
    memberColor = new Array,
    mode = 0,
    selected = 0,
    channel;
variables = { choices, Guild, Guilds, Channels, ChannelsId, ChannelsType, selectCursor, limit, chat, memberColor, channel, mode, selected }
exports.variables = variables